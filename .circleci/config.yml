version: 2.1
orbs:
  node: circleci/node@4.7.0
jobs:
  build:
    docker:
      - image: cimg/node:lts
    working_directory: ~/src
    steps:
      - checkout
      - run: npm install
      - run: npm run test
